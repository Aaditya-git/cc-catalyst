{
  "name": "cc-catalyst",
  "version": "0.3.3",
  "description": "Session intelligence for Claude Code — active token management via skills and hooks",
  "main": "dist/cli/index.js",
  "bin": {
    "cc-catalyst": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && chmod +x dist/cli/index.js",
    "build:watch": "tsc --watch",
    "cli": "node dist/cli/index.js",
    "preuninstall": "node dist/cli/index.js remove || true",
    "prepublishOnly": "npm test && npm run build",
    "test": "jest --testPathPattern='tests/unit'",
    "test:integration": "jest --testPathPattern='tests/integration'",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "claude",
    "claude-code",
    "token",
    "optimization",
    "skills",
    "hooks",
    "anthropic"
  ],
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
